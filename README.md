Django-Vue TODO
===============

Toy multi-tenant to-do app using Django and Vue.

## Backend

REST resources for todo-management. Start the server:

```bash
# in ./backend
python manage.py runserver
```